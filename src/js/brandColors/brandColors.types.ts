export interface Color {
  [key: string]: string;
}

export interface BrandColors {
  white: Color;
  black: Color;
  grey: Color;
  blue: Color;
  orange: Color;
  green: Color;
  red: Color;
  purple: Color;
  violet: Color;
  yellow: Color;
}
