import { lightTheme, darkTheme } from '../themes';

/**
 * This object is DEPRECATED in favour of the theme objects
 */

export const colors = {
  light: {
    ...lightTheme.colors,
  },
  dark: {
    ...darkTheme.colors,
  },
};
